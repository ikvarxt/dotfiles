-- Every `*.lua` file in this directory is imported by lazy.nvim (see
-- `lua/config/lazy.lua`). Add a plugin by creating a file here that returns its
-- spec -- no registration step needed.
--
-- See `:help lazy.nvim-🔌-plugin-spec` for everything a spec can hold.
return {}
